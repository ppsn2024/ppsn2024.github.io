var slider = new IdealImageSlider.Slider({
    selector: '#slider',
    height: 400, // Required but can be set by CSS
    interval: 5000
});
slider.start();

var parkhotel_slider = new IdealImageSlider.Slider({
    selector: '#parkhotel_slider',
    height: 400, // Required but can be set by CSS
    interval: 5000
});
parkhotel_slider.start();

var aec_slider = new IdealImageSlider.Slider({
    selector: '#aec_slider',
    height: 400, // Required but can be set by CSS
    interval: 5000
});
aec_slider.start();

var freistadt_slider = new IdealImageSlider.Slider({
    selector: '#freistadt_slider',
    height: 400, // Required but can be set by CSS
    interval: 5000
});
freistadt_slider.start();